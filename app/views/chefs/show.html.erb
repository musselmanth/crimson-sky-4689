<h1>Chef <%= @chef.name %></h1>
<%= link_to "View a List of Ingredients #{@chef.name} Uses", chef_ingredients_path(@chef.id) %>
<h3>Most Popular Ingredients In Chef <%= @chef.name %>'s Dishes:</h3>
<ul id="most_popular_ingredients_list">
  <% @most_popular_ingredients.each do |ingredient| %>
    <li id="ingredient_<%= ingredient.id %>" >
      <%= ingredient.name %>
    </li>
  <% end %>
</ul>